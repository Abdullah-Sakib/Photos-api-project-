/*
1. map() method loop all the elements of an array and do something with every    element and then store the elements in another array.

2. forEach() method loop all the element of an array and do something for every element. It does not store the elements like map() method.

3. filter() method filters elements of an array and store them in another array.
we must provide a condition in the filter() method. Which elements will fullfil the condition they will be stored in another array.

4. find() method find element from an array. We provide a condition and the element which fullfill the condition get returned.
*/